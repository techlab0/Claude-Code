{
  "name": "claude-code-project",
  "version": "1.0.0",
  "description": "Simple HTML project with live server",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html",
    "start": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}